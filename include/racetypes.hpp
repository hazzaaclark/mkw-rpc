#pragma once
#include <map>

typedef enum : uint32_t
{
	GRAND_PRIX,
	TIME_TRIALS,
	SOLO_VS_RACE,
	TEAM_VS_RACE,
	COIN_BATTLE,
	BALLOON_BATTLE,
} RaceType;

typedef enum : uint32_t
{

};
